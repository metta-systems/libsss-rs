use pnet_macros_support::types::*;

#[packet]
pub struct Cookie {
    pub magic: u64be,
    #[length = "16"]
    pub compressed_nonce: Vec<u8>,
    #[payload]
    #[length = "144"]
    pub payload: Vec<u8>,
}

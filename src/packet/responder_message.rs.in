use pnet_macros_support::types::*;

#[packet]
pub struct ResponderMessage {
    pub magic: u64be,
    #[length = "32"]
    pub responder_shortterm_public_key: Vec<u8>,
    pub compressed_nonce: u64be,
    #[payload]
    pub payload: Vec<u8>,
}
